/**
 * @deprecated Use usePreviewStore().activeTheme instead
 */
export { usePreviewStore as previewThemeSource } from "@/hooks/use-preview-store";

import type { CardThemeId } from "./card-themes";

export const ACTIVE_THEME: CardThemeId = "cyber-gaming";
