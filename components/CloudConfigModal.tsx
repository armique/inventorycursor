
// This component is no longer used in Server Sync mode.
// Kept empty to satisfy imports if any remain, but can be safely deleted.
import React from 'react';
export default function CloudConfigModal({ onClose }: { onClose: () => void }) { return null; }
